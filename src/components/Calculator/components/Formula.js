import React from 'react';
import Scrolless from './Scrolless';

const Formula = ({ output }) => {
  return (
    <div id="formula">
      <Scrolless output={output} />
    </div>
  );
};

export default Formula;